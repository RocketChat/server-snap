#! /bin/bash

# If the service is stopped manually,
# write this file to signal the post-stop script
# to skip the 5-second sleep.
pid=$(pgrep -f "node /snap/rocketchat-server/.+/main.js")
if [[ -n $pid ]]
then
    rcstop="$SNAP_COMMON/rcstop"
    touch $rcstop
    echo "$rcstop written!"
fi
